# Runtime network config
# Platform-provided PORT should take precedence in production.
APP_PORT=8080
CSS_PORT=3000
HOST=0.0.0.0

# Solid/CSS config (replace for your environment)
SOLID_POD_SERVER_URL=http://localhost:${CSS_PORT}
SOLID_OIDC_ISSUER=http://localhost:${CSS_PORT}
SOLID_POD_PATH=/health-pim/
SOLID_REDIRECT_URL=http://localhost:${APP_PORT}/callback
